name: Tests

on:
  workflow_dispatch:
  pull_request:
    paths-ignore:
      - "docs/**"
  push:
    branches: ["main"]
    paths-ignore:
      - "docs/**"
  schedule:
    # cron every week on monday
    - cron: "0 0 * * 1"

permissions:
  contents: read
  statuses: write

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.12", "3.13", "3.14"]

    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      rabbitmq:
        image: rabbitmq:3.13-management
        env:
          RABBITMQ_DEFAULT_USER: rabbit
          RABBITMQ_DEFAULT_PASS: rabbit-insecure-for-tests-only-1234567890
        ports:
          - 5672:5672
        options: >-
          --health-cmd "rabbitmq-diagnostics status"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/test_db
      REDIS_URL: redis://localhost:6379/0
      CELERY_BROKER_URL: amqp://rabbit:rabbit-insecure-for-tests-only-1234567890@rabbitmq:5672//
      DJANGO_SETTINGS_MODULE: dvi_user_service.settings.test
      DJANGO_ENVIRONMENT: test
      SECRET_KEY: django-insecure-for-tests-only-1234567890

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Check for missing migrations
        run: python manage.py makemigrations --check --dry-run

      - name: Run tests with coverage
        run: pytest -v --cov=users --cov-branch --cov-fail-under=80

      - name: Prepare coverage data for combining
        run: mv .coverage .coverage.${{ matrix.os }}.${{ matrix.python-version }}

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.os }}-${{ matrix.python-version }}
          path: .coverage.*
          include-hidden-files: true

  coverage-combine:
    needs: ["test"]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download all coverage artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: coverage-*
          merge-multiple: true

      - name: Combine & report
        run: |
          pip install coverage
          coverage combine .coverage*
          coverage report --fail-under=80
          coverage html

      - name: Extract coverage percentage
        id: cov
        run: |
          TOTAL=$(coverage report | tail -1 | awk '{print $NF}')
          echo "total=$TOTAL" >> $GITHUB_OUTPUT

      - name: Update coverage Gist
        uses: schneegans/dynamic-badges-action@v1.7.0
        with:
          auth: ${{ secrets.GIST_TOKEN }}
          gistID: ef41d8165e52e674ad2fca6e6ef06da9
          filename: coverage.json
          label: Coverage
          message: ${{ steps.cov.outputs.total }}
          valColorRange: ${{ steps.cov.outputs.total }}
          maxColorRange: 100
          minColorRange: 0

      - name: Upload HTML report via smokeshow
        run: |
          pip install smokeshow
          smokeshow upload htmlcov
        env:
          SMOKESHOW_GITHUB_STATUS_DESCRIPTION: Coverage {coverage-percentage}
          SMOKESHOW_GITHUB_COVERAGE_THRESHOLD: 80
          SMOKESHOW_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SMOKESHOW_AUTH_KEY: ${{ secrets.SMOKESHOW_AUTH_KEY }}
